- defaults:
        link:
                relink: true
                create: true
                relative: true

- link:
        ~/.pam_environment:
        ~/.xinitrc:
        ~/.xserverrc:
        ~/.zshenv:
        ~/.config/git:
                path: config/git
        ~/.config/i3:
                path: config/i3
        ~/.config/termite:
                path: config/termite
        ~/.config/zsh:
                path: config/zsh
